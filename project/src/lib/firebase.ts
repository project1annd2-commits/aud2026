// Firebase initialization removed as we have migrated to a local MongoDB backend.
// Keeping the file structure to avoid breaking imports but exporting mocks/nulls.

export const db = {} as any;
export const auth = {} as any;

const app = {} as any;
export default app;